@model List<Item>

<h1>Shopping Cart</h1>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Image</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Price</td>
                <td><img src="@item.ImageUrl" width="100" height="100"/></td>
                <td><button onclick="purchase('@item.Id')">Buy Product</button></td>
            </tr>
        }
    </tbody>
</table>
<script src="/lib/jquery/dist/jquery.js"></script>
<script>


    function purchase(itemId) {
        itemId = parseInt(itemId)
        let xml = new XMLHttpRequest();
        xml.open("post", `ShoppingCart/Purchase/?id=${itemId}`)
        xml.onreadystatechange = function () {
            if (this.status == 200 && this.readyState == 4) {
                alert(this.responseText)
            }
        }
        xml.send()
        
       /* $.post("ShoppingCart/Purchase", {
            id:itemId
        },
            function (data,status) {
                switch(data){
                    case "Success":
                        alert("success");
                        break;
                    default:
                        alert("error");
                        break;
                }
            }

        )*/
    }
</script>